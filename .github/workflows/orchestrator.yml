name: Orchestrator
on:
  workflow_dispatch:
  #schedule:
  #  - cron: "45 9 1 */3 *"


env:
  IDLE_TIME: 60

jobs:
  run-spreadsheet:
    uses: ./.github/workflows/spreadsheet.yml

  run-mf-workflow:
    needs: run-spreadsheet
    uses: ./.github/workflows/MF.yml
  
  run-email-workflow:
    needs: run-mf-workflow
    uses: ./.github/workflows/Email.yml
    secrets: inherit
