name: Orchestrator
on:
  workflow_dispatch:
  schedule:
    - cron: "38 2 1 */3 *"
    - cron: "38 18 10 * *"


env:
  IDLE_TIME: 60

jobs:
  run-mf-workflow:
    uses: ./.github/workflows/MF.yml
  
  run-email-workflow:
    needs: run-mf-workflow
    uses: ./.github/workflows/Email.yml
    secrets: inherit
